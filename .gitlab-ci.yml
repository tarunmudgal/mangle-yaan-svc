variables:
  DOCKER_HOST: tcp://localhost:2375/
  DOCKER_DRIVER: overlay2

services:
  - docker:18.09-dind

build-and-deploy:
  stage: build
  image: docker:18.09
  script:
    - echo "Running Build and Deploy Script"
    - sh ./build_deploy.sh
  only:
    - master
  when: on_success
